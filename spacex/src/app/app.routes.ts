import { Routes } from '@angular/router';
import { SpacexLaunchesComponent } from './spacex-launchers/spacex-launchers.component';

export const routes: Routes = [
  { path: '', component: SpacexLaunchesComponent } 
];